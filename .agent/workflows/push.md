---
description: Push all local changes to the remote repository
---
1. Stage all changes
// turbo
`git add .`

2. Commit with a message. If the user hasn't provided one, I will generate a descriptive message based on the changes.
// turbo
`git commit -m "update: blog posts and assets"`

3. Push to the remote branch
// turbo
`git push`
